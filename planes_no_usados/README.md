# Planes NO usados (descartados / reemplazados)

> ⛔ **NO tomar en cuenta.** Estos planes quedaron descartados o fueron reemplazados.
> No los analices, no los ejecutes, no los uses como referencia de arquitectura.
> El plan de arquitectura vigente es [`../plan_arquitectura_v3.md`](../plan_arquitectura_v3.md).

- `plan_jesus_altur-1.md` — plan viejo (CPU-only + TigerData + FHE/voiceprints). Descartado: los voiceprints sobreajustan a hablantes y el FHE resuelve verificación con enrolamiento, que este reto no tiene.
- `plan_arquitectura_emilio-1.docx` — arquitectura multi-cloud original (GCP/Snowflake/Vultr/TigerGraph). Reemplazada por el enfoque híbrido de `plan_arquitectura_v3.md`.
- `PLAN_PARA_CLAUDE.md` — brief de implementación anterior (Triple Ensemble). Reemplazado por `plan_arquitectura_v3.md` y la implementación ya en `src/`.
