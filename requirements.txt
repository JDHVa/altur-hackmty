# --- Core / Senal A (tabular, CPU) ---
pandas
numpy
scikit-learn
lightgbm
xgboost
joblib

# --- Senal B (audio) ---
torch
torchaudio
torchvision
librosa
soundfile
sounddevice            # grabar audio humano propio (scripts/record_human.py)
webrtcvad-wheels       # VAD para derivar turnos en /detect (mejor que energia)
tqdm
transformers            # Wav2Vec2/WavLM (modelo SOTA, tarea Camino B)
praat-parselmouth       # jitter/shimmer/HNR (features bio, plan v4 B-B)
imageio-ffmpeg          # ffmpeg embebido: codecs telefonicos (plan v4 B-A)

# --- Senal C (semantico / LLM) ---
faster-whisper
edge-tts                # TTS es-MX para dataset sintetico (plan v4 B-C)

# --- API POST /detect ---
fastapi
uvicorn
pydantic
python-multipart
